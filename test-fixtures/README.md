# test-fixtures — golden corpus for parity tests

> **Status:** empty. A fresh corpus is recorded against a neutral demo
> app served by the test harness. Shape of each fixture:
>
>     test-fixtures/scenario/<name>/
>     ├── scenario.json
>     ├── snapshots/   screenshots/   network/   probes/
>     └── replays/<runId>/...
>
> See `docs/specs/scenario-sidecar-tree.md` for the on-disk contract.
